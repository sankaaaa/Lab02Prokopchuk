<UserControl x:Class="Lab02Prokopchuk.Views.UserDataView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Height="400" Width="200">
    <Grid>
        <StackPanel>
            <TextBlock Text="Name:" Margin="0, 5, 0, 0"/>
            <TextBox Text="{Binding Name}"/>
            <TextBlock Text="Last Name:" Margin="0, 5, 0, 0"/>
            <TextBox Text="{Binding Lastname}"/>
            <TextBlock Text="Email:" Margin="0, 5, 0, 0"/>
            <TextBox Text="{Binding Email}"/>
            <TextBlock Text="Birth Date:" Margin="0, 5, 0, 0"/>
            <DatePicker Name="datePicker" SelectedDateFormat="Short" SelectedDateChanged="DatePicker_SelectedDateChanged"/>
            <Button Name="BAboutBirthday" Content="Proceed" Margin="0,5,0,0" Command="{Binding ProceedCommand}" IsEnabled="{Binding ProceedEnabled}"/>
            <TextBlock Text="{Binding Information}" Margin="0,10,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
